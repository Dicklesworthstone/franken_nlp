# Changelog

All notable changes to `franken_nlp` will be recorded here.

The project is currently in design review. There is no executable release yet.

## [Unreleased]

### Planning

- Defined the one-model Rust library and `fnlp` CLI target around Nanbeige4.2-3B.
- Specified the pinned-source truth pack, loop/KV/norm conformance ladder, custom `.fnlpq` format, CPU kernel campaign, bounded batch engine, and staged NLP task layer.
- Added first-class Apple M4/M5, exact Zen 3 AVX2, and measured Zen 4/5/Intel AVX-512-VNNI design tracks.
- Added artifact-integrity, privacy, resource-admission, and evidence-state requirements.
- Froze direct non-FrankenSuite release dependencies to the owner-approved `clap`, `serde`/`serde_json`, and `sha2` families.
- Added exact constrained-execution primitives (all-legal row projection, forced causal runs, byte-exact source languages), continuation-trie scoring, and their universal differential-tested fallbacks.
- Added opt-in durable corpus jobs, user-owned evaluation/calibration/qualification, bounded data-only TaskIR recipes, and explicit authority/privacy boundaries.
- Added profile-gated alien-artifact recommendation cards for certified kernel search, joint quantization allocation, communication-avoiding scheduling, trace-gated cross-loop wavefront research, caching, tail-quality control, uncertainty, and exact loop-draft research.
- Preserved both idea-wizard records and added an authoritative disposition table rejecting confidence theater, free-margin claims, premature adaptive escalation, and unmeasured prompt-layout speedups.
- Clarified that forced execution requires one legal token id—not merely one byte continuation—and narrowed per-install tuning to durable, validity-domain-keyed bit-identical choices.
- Added a typed untrusted-document/control-token boundary, empirical content-steering scorecards, an evidence-gated same-model semantic second reader, and AA-A1 human-authorized frozen-corpus acceptance sampling.
- Added item-local/partition-reduce/corpus-global cache authority, snapshot-qualified entity resolution, and staged portable partition/merge lineage work.
- Added byte-certified KV fork-tail admission and exact breadth/depth-first/short-ID/naïve continuation-scoring alternatives for the model's unusually expensive 44-deep cache.
- Added AA-R1 as trace-gated local resident research for agent-heavy hosts, explicitly without authorizing a routable inference server.
- Preserved both adversarial cross-scores and both post-duel reaction records with prominent notices distinguishing historical snapshots from the normative v4 plan.
- Traced FrankenOCR's shipped Baidu model lifecycle and made the Nanbeige equivalent normative: immutable local source fetch, deterministic Generic `.fnlpq` conversion receipts, independent model-artifact versioning, exact 1,957,046,720-byte GitHub Release chunks, release-bound manifests, streamed `fnlp pull` verification/reassembly/native packing, platform cache paths, and installer delegation to the one Rust artifact manager.
- Settled model licensing as the declared Apache-2.0 (official model-card metadata at the pinned revision, per owner ruling): removed the former fail-closed LG-1 publication gate everywhere; compliance is the mechanical §5.7 checklist — every artifact/release carries the Apache-2.0 text, Nanbeige attribution, and modification notice, byte-compared by a release test. `swiss_army_llama` is the owner's own prior work; its sentiment methodology/presets are reused directly with attribution.
- Restored the README to the FrankenSuite as-if-realized voice (the franken_lean form): present tense with an explicit note on tense, trued up as phase gates land; performance numbers remain TARGETED-not-OBSERVED until measured.
- Completed independent adversarial review round 2: corrected exact tensor-payload versus safetensors-container arithmetic; replaced the obsolete fork-only premise with official llama.cpp's 2026-07-27 Nanbeige4.2 support as the primary CPU/GGUF baseline; recorded implementation lineage and GPL RLX boundaries without inflating oracle independence.
- Hardened the artifact lifecycle with canonical serialization, cross-OS/ISA digest certification or a named canonical-publisher fallback, measured converter peak space, publisher attestations distinct from checksums, owner-controlled `create_new`/lock/sync/rename transactions, native-pack crash recovery, hostile-root tests, and honest revocation/durability limits.
- Specified addressable seeded sampling, exact top-p requirements, HMAC-SHA-256 pseudonyms and secret handling, narrowly versioned text utilities, typed robot flush/error events, canonical output conditions, completeness-graded `.fnlpr` receipts, public-claims/behavior/structural-cost registries, a simple scalar specification engine, and Phase-0 executable architectural ratification probes.
- Expanded the profile-gated optimization queue with conservative alien/graveyard candidates: cache-pollution-resistant M=1 weight streaming/prefetch, exact radix/logit ordering, and deterministic MinHash/LSH entity-resolution blocking, each with an explicit loss, proof/quality gate, and simple fallback; compact masks/scanners remain symptom-routed, while epoch reclamation is rejected as a presumed default.
- Corrected execution ownership after auditing the pinned foundations: FrankenNLP uses asupersync `Cx::spawn_blocking` plus whole-request/batch-epoch `Cx::scoped_cpu` teams for intra-op scheduling; leaf kernels never spawn, frankentorch must expose a no-Rayon serial/range surface, and Rayon is absent from the release graph. Added team-width, checkpoint, join, re-entry, startup-cost, and dependency-graph G0 gates.
- Tightened sampling/pseudonym/receipt/cost semantics: semantic request digests now address RNG draws; pseudonym keys require high entropy, carry a nonsecret resume commitment, and handle truncation collisions before emission; receipts omit private bytes by default; structural witnesses distinguish full two-loop target calls from optional loop-1 draft work.
- Closed second-order scheduler/resource holes: confirmed asupersync's own Rayon edge is dev-only; each request/finite epoch owns an asupersync region; fixed teams form and spawn-seal before request work begins; already-running blocking closures stay supervised and retain admission, memory, and output ownership until actual scoped-team completion even when a wrapper task is cancelled; topology/memory/NUMA/QoS authority must come from a ratified safe FrankenSuite surface or fail/fall back explicitly; activation-journal forks fail closed.
- Separated implementation parity from deliberate quantization distortion: `hf-bf16-eager` owns exact HF-token claims on oracle-reproducible prefixes, `diagnostic-f32` is a structural/bisect profile rather than a false HF-token oracle, optimized paths preserve the same recipe's scalar semantics, and int8/int4 divergence from bf16 is governed by explicit logit/token/task gates instead of an impossible blanket token-identity promise.
- Made generation behavior and native artifacts unambiguous: `BN-GEN-DEFAULT-001` records fnlp's intentional greedy default versus upstream sampling, seeds have one canonical 256-bit schedule, `fnlp convert` emits Generic only, and host packings are derived through `fnlp models derive`.
- Corrected the malformed immutable MIT-RLX evidence pin after live commit-resolution and external-link checks; the GPL-3.0 implementation remains an out-of-tree differential only.
- Integrated external review round 3 (GPT Pro deep reasoning, 44 proposals): scoped every fidelity claim by a named numerics profile — the pinned oracle executes bf16 with explicit cast points, so `hf-bf16-eager` owns HF-match claims while `diagnostic-f32` is the structural oracle and quantized profiles carry preregistered budgets — and specified the non-perturbing forward-hook protocol required because standard HF hidden-state output surfaces only the final loop of the 44-execution ladder.
- Replaced runtime weight-quantization kill-switch fiction with staged immutable artifacts (`int8-mlp` → `int8-mlp-attn` → `int8-all`; rollback activates the prior artifact), unified all cache/job/receipt keys under one canonical `ExecutionIdentity` with a compatibility registry, named the three candidate-scoring probability spaces with mandatory `score_space` labeling, and made batching compatibility operator-specific so attention shape can no longer destroy linear-stage weight-stream amortization.
- Hardened boundaries the review flagged: template-control tokens censused independently of the tokenizer `special` flag; the full chat-message AST (content parts, media reminders, default system text, reasoning content, tool-result grouping) made normative; the independent validator moved out of the grammar module with bounded JSON numbers and duplicate-key rejection everywhere; per-document source automata excluded from global caches; owned-job content identified by keyed HMAC commitments instead of raw hashes; and the spool-first/journal-second durable commit protocol specified.
- Corrected buildability and honesty details: suite root Cargo patches mirrored via a checked closure since Cargo ignores dependency-root patches; exact dated nightly pinned; the in-process "guarded smoke instruction" replaced with detection-gated execution; logical weight-read volume distinguished from measured DRAM traffic; llama.cpp comparisons renamed to nearest-format peers; KV logical pages decomposed into bounded physical slabs with scale-overhead arithmetic; the memory budget decomposed into an exact admission certificate; a cold-start R0 regime added; summarize/answer citation guarantees renamed `--cited` with `grounded` reserved for semantically qualified versions; `--tools` blocked until the tool-parser contract is evidenced; exit code 10 added for typed task no-results; and the CLI table gained per-subcommand phase eligibility.
- Extended the verification program: Phase 4 split into gated sub-phases 4A/4B/4C; Phase −1/0 exits strengthened (materialized config, oracle numerics matrix, trace-harness equality, benchmark-conflict record, platform-surface registry); new evals for resolve, keyphrases, tool parsing, text utilities, per-detector redaction, and TaskIR recipes; license mechanics tightened to archive exact upstream notices and never synthesize a copyright line, with `fnlp licenses` exposing the bundle; release attestations extended to SBOM and offline verification; and the research register extended with OQ-30 through OQ-34 plus twelve new risk rows.
- Affirmed the Apple integrated GPU as an explicit owner-ruled acceleration track: Metal via `ft-kernel-metal` follows the parity-proven CPU core, prefill first, under its own named numerics profile; CPU remains the reference implementation and portable floor, and CUDA stays out of scope.
- Integrated independent fresh-eyes review round 4 as plan v4.2: corrected the embedding reference dtype and an invalid `.fnlpq` Rust magic literal; separated computational-model, packing, source, legal-bundle, and physical-file digests; replaced remaining fictional kill-switch wording; made asupersync capability narrowing, resource budgets, four-way outcomes, quiescence, and native-thread proof boundaries explicit; strengthened oracle repeatability sampling and Rust-2024 unsafe discipline; corrected binary-count, privacy, calibration, license-attribution, Metal-scope, and historical-provenance wording. Because these were material corrections, v4.2 remains a steady-state candidate pending one clean independent confirmation pass.
- Completed the owner-directed asupersync leverage pass (v4.3): re-grounded the async halves of the scheduler, daemon, job, and pull test programs on the pinned foundation's deterministic lab (seed-bound chaos, bounded DPOR-style exploration, quiescence/obligation-leak/cancellation-protocol oracles, obligation-based futurelock detection, crashpack replay artifacts, `VirtualTcp` network fixtures, and concrete TLA+ export for separate TLC analysis) while the bounded native team-state model retains the `scoped_cpu` OS-thread half; named the runtime presets and control knobs `EngineResources` must fix, including an explicit obligation-leak response policy; introduced candidate reserve-then-permit, retry, `first_ok`, and `bracket` uses subject to the pin-accurate contract corrections recorded in v4.4; named the batch daemon and any AA-R1 resident as supervision/GenServer candidates with registry name-leases; put the asupersync-workspace substrate crates (`franken_kernel`, `franken_evidence`, `franken_decision`, `frankenlab`) into the dependency audit; bound concurrency-failure receipts to lab seed, trace fingerprint, and crashpack id; mapped exit codes onto the four-valued outcome and structured cancel kinds; and added the G0/OQ-35 leverage-surface census that ratifies each facility at the pin or records the hand-rolled design as the named fallback.
- Corrected the v4.3 foundation mapping in the v4.4 confirmation pass: `first_ok` is sequential rather than a concurrent first-success race; `bracket` has bounded best-effort drop cleanup rather than a durable release guarantee; GenServer `cast` acknowledges enqueue, not processing or commit; DPOR-style exploration is bounded coverage and a TLA+ export is only an input to a retained TLC run; futurelock reports are bounded witnesses rather than deadlock proofs; all eleven `CancelKind` variants survive to the policy boundary; and process admission now budgets runtime workers, blocking coordinators, scoped CPU children, and helper threads together. Also standardized `fnlp convert` on `--recipe`; recorded the exact ten-file, 8,360,887,509-byte source-conversion closure; corrected executable, CLI-example, CUDA/POS-scope, recovery-cost, and physical-weight-streaming overclaims; defined mathematical numeric equality and canonical spelling for bounded JSON-Schema `enum`/`const`; narrowed the M=1 cache experiment to architecturally valid prefetch/cache-management mechanisms; made private evidence receipts use keyed commitments instead of raw low-entropy request hashes; added per-mirror child budgets and a GitHub-host availability fallback; corrected the Beads JSONL path and preimplementation swarm-performance claims; and reset the clean-confirmation requirement because these were material findings.
